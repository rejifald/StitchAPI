{
    "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2022", "DOM"],
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "esModuleInterop": true,
        "forceConsistentCasingInFileNames": true,
        "skipLibCheck": true,
        "noEmit": true,
        "strict": true
    },
    "include": ["src/**/*.ts"],
    "comment-exclude": "Shipped source must type-check dependency-free. Test files (*.test.ts) use node:assert + process and are verified by execution (tsx) now; their static type-check + strict-null cleanup is owned by task T-α and runs under pnpm install in CI.",
    "exclude": ["**/*.test.ts"]
}
